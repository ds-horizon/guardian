<configuration scan="true" scanPeriod="10 seconds">
  <property name="artifactId" value="guardian"/>
  <property name="log.directory.path" value="/var/logs"/>
  <property name="log.file.name" value="guardian.log"/>

  <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>${log.directory.path}/${log.file.name}</file>
    <append>true</append>
    <encoder>
      <pattern>
        %date %highlight(%+5level) %-26thread %X{dd.trace_id:-0} %X{dd.span_id:-0} %logger{36} [%file:%line] %green(%msg) %n
      </pattern>
    </encoder>

    <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
      <fileNamePattern>${log.directory.path}/archived/guardian.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
      <maxFileSize>512MB</maxFileSize>
      <maxHistory>7</maxHistory>
      <totalSizeCap>5GB</totalSizeCap>
    </rollingPolicy>
  </appender>

  <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
    <appender-ref ref="FILE"/>
    <includeCallerData>${includeCallerData:-false}</includeCallerData>
    <neverBlock>true</neverBlock>
  </appender>

  <logger name="com.dreamsportslabs" level="info"/>
  <logger name="com.dream11" level="debug"/>

  <root level="info">
    <appender-ref ref="ASYNC_FILE"/>
  </root>

</configuration>